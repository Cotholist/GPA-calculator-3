{
  "name": "@gpa-calculator/frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && copy src\\*.html dist\\",
    "deploy": "wrangler pages deploy dist"
  }
}
