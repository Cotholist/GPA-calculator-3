{
  "name": "@gpa-calculator/frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cp -r src/* dist/",
    "dev": "http-server src",
    "deploy": "wrangler pages deploy dist"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
